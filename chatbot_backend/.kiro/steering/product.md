# Product Overview

Chatbot Workflow Engine — a backend service for Xpressbees that manages chatbot conversation workflows. It provides a REST API to create, read, update, and delete workflow definitions stored as JSON structures in PostgreSQL.

Workflows represent conversational flows (nodes, edges, conditions) that drive a chatbot's behavior. The system is currently in early development with Phase 2 (WebSocket support) planned.
